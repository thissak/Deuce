# Deuce에 기여하기

Deuce는 사람과 각자의 AI가 채널에서 함께 대화하는 메신저를 목표로 합니다.
버그 재현, 문서 개선, 사용성 제안, 코드 기여를 환영합니다. 큰 기능은 구현 전에
이슈에서 사용 상황과 범위를 맞춰 주세요. 현재는 채팅 중심이며 영상통화는 제외합니다.

## 개발 흐름

1. [로컬 개발](docs/getting-started.md)에 따라 자신의 DB와 개발 환경을 준비합니다.
2. 작업 브랜치에서 변경합니다. 운영 서버나 운영 사용자의 데이터는 필요하지 않습니다.
3. `pnpm test`, `pnpm typecheck`, `pnpm build`를 실행합니다.
4. 문제·변경 후 동작·검증 결과를 적어 PR을 엽니다. DB 변경이면 migration을 포함합니다.
5. 유지관리자 검토와 CI를 통과한 뒤 병합합니다. 배포는 유지관리자가 별도로 수행합니다.

문서는 한국어를 기본으로 하며 이슈·PR은 한국어와 영어 모두 가능합니다.
개인의 글로벌 AI 설정, 사내 도구, GCP 계정은 기여에 필요하지 않습니다.

## 테스트와 문서

회귀 위험이 있는 동작에는 실패 상황을 검증하는 테스트를 추가합니다. 문구·문서 같은
단순 변경에 불필요한 테스트를 만들지 않습니다. 서버 테스트는 `deuce_test`의 데이터를
삭제하므로 이 DB에는 실제 데이터를 넣지 마세요. Google 응답은 테스트에서 모의합니다.

타입 계약은 `packages/shared`, 상태는 `docs/PROGRESS.md`, 변경 기록은
`docs/CHANGELOG.md`가 원본입니다. 중요한 구조 변경은 `docs/adr/`에 이유를 남깁니다.

## 공유할 정보

`.env`, Google OAuth JSON, MCP 연결 파일, 인증 헤더, 운영 DB·첨부·로그 원본은
커밋하지 않습니다. 재현에는 `example.com` 주소와 가짜 데이터를 사용하세요.
진단 보고서의 식별자도 비식별화합니다. 로컬 증빙 `docs/handoff/*-receipt.json`과
`.private/`는 공개 대상에서 제외합니다.

PR 코드는 GitHub 호스팅 러너에서 실행합니다. CI에 운영 VM 권한이나 배포용 비밀정보를
주지 않습니다. 외부 PR 코드에 운영 자격증명이 전달되는 작업을 추가하지 마세요.

기여하는 코드와 자료에 대한 권한을 확인해 주세요. 기여물에는 [Apache-2.0](LICENSE)을
적용하며 외부 코드·자산의 출처와 라이선스를 보존합니다.
