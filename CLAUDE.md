# Deuce (듀스)

골든랩 구성원 10명 이하가 바로 사용하는 내부 협업 도구다. 사람과 에이전트가
같은 대화, 자료, 작업 맥락을 보고 채널과 스레드 안에서 함께 일하는 공간을
목표로 한다.

## 핵심 계약

- 사람과 에이전트는 동일한 워크스페이스 데이터 계약을 사용한다.
- 메시지와 원본 파일이 SSOT이며, 요약·추출 텍스트·임베딩은 재생성 가능한 파생 데이터로 취급한다.
- 에이전트의 답변과 작업 결과에는 사용한 자료와 대화의 출처를 남긴다.
- 에이전트 실행 과정과 상태는 사용자에게 보이게 기록한다.
- 에이전트 간 연쇄 호출에는 단계·시간·비용 제한을 두고, 삭제·외부 전송·배포는 사람 승인을 받는다.
- 현재 상태의 SSOT는 `docs/PROGRESS.md`, 변경 이력의 SSOT는 `docs/CHANGELOG.md`다.

## 작업 진입

1. 글로벌 `~/.claude/CLAUDE.md`를 끝까지 읽는다.
2. 이 `CLAUDE.md`를 읽는다.
3. `docs/PROGRESS.md`, 이어서 `docs/CHANGELOG.md`를 읽는다.
4. `git status --short --branch`로 사용자 작업을 확인한다.

## 안전 경계

- 인증, 동시성, 큐, 재시도, 캐시, 상태 동기화, 마이그레이션은 기존 라이브러리와 prior art를 먼저 조사한 뒤 설계한다.
- 인프라 변경, 외부 서비스 연결, 원격 저장소 생성, push, 배포는 감독이 명시적으로 승인한 범위에서만 수행한다.
- 작업 종료와 커밋 전에 글로벌 `update-docs`로 문서를 점검한다.
