{
  "name": "deuce",
  "private": true,
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "dev": "pnpm --parallel --filter @deuce/server --filter @deuce/web dev",
    "build": "pnpm --filter @deuce/web build && pnpm --filter @deuce/mcp build"
  },
  "engines": {
    "node": ">=24 <27"
  }
}
