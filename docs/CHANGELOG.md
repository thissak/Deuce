# Changelog

## Unreleased

- [fix] 타임라인 맨 위 메시지의 더보기 메뉴·반응 팔레트가 스크롤 영역 위로 잘려 클릭할 수 없던 0.2.4 회귀 수정. 위쪽 공간이 팝오버보다 좁으면 아래로 연다(`open-below`). PR #19 Codex 듀얼 리뷰가 찾은 결함.
- [docs] README·self-hosting의 AI 제거 "미배포" 문구를 0.2.4 배포 기록에 맞춤.
- [feat] 왼쪽 채팅 목록을 `전체·읽지 않음·대화·채널`로 분리해 1:1·그룹 대화와 채널을 따로 볼 수 있게 함. 각 항목의 우클릭·`⋯`·Shift+F10 메뉴에서 음소거/해제와 그룹·채널 나가기를 제공하고, 나가기는 확인 후 기존 서버 API로 처리.

## 0.2.4 — 2026-09-11

- [remove] #18 AI 초대·설정·요청 UI, 서버 AI API·MCP·실행기 소켓, 데스크톱 실행기와 전용 패키지 제거. 사람 멘션과 과거 AI 메시지 표시는 유지.
- [migration] 기존 연결 키 7개를 회수하고 기본 AI 설정 1개를 해제했으며 진행 중 요청을 종료. 연결·메시지 95개·권한·완료 기록과 테이블은 보존.
- [fix] 메시지 호버 액션 바를 GateLab과 같은 말풍선 위 경계로 이동. 내 메시지는 오른쪽, 상대 메시지는 왼쪽에 맞추고 반응 팔레트·더보기 메뉴도 위로 열어 본문을 가리지 않게 수정.
- [test] 웹 155·서버 79·데스크톱 10개(총 244개), 전체 타입 검사·빌드와 운영 복제 DB migration 검증 통과. 구 AI API·MCP 404, 실행기 연결 거부, 데이터 보존과 사람 멘션 회귀를 확인.
- [release] 운영 웹 `20260911-02`와 Mac arm64/x64·Windows x64 `0.2.4`, 업데이트 피드를 배포. 운영 백업, Mac 서명·Apple 공증·staple·Gatekeeper·DMG, 공개 파일 58개 전체 SHA-256과 HTTP 점검 8개를 확인. 설치 Mac도 0.2.3→0.2.4 업데이트 후 AI 제어가 없는 화면과 액션 바 비겹침을 확인. [배포 기록](handoff/2026-09-11-ai-retirement-release.md).
- [docs] 감독의 현재 작업 세션 연결 요구를 확정. 공식 App Server·Remote·MCP 문서를 조사해 동일 대화의 조회·재개·입출력 기능과 기존 데스크톱 대화 연결의 미확인 경계를 기록하고 PoC 순서를 구체화.

## 0.2.3 — 2026-09-11

- [release] 웹 `20260911-01`과 Mac arm64/x64·Windows x64 `0.2.3`, 두 업데이트 피드를 함께 공개. 운영 백업·health 확인, Mac 서명·Apple 공증·Gatekeeper·DMG와 패키지 코드·피드 해시 검증 통과. 서버·운영 DB 변경 없음. 공개 HTTPS 파일 58개 전체 SHA-256·HTTP 점검 6개 통과. [배포 기록](handoff/2026-09-11-mention-release.md).

- [test] 멘션 변경: 웹 162·데스크톱 15·MCP 2·실행기 6개와 VM 격리 DB 서버 94개(총 279개), 전체 타입 검사·빌드 통과. 로컬 DB 연결 실패는 VM의 별도 임시 DB 검증으로 보완했다. 실제 Chromium에서 키보드 선택·스크롤·Esc·이름 색상 표시를 확인했다.
- [feat] 채팅 멘션을 전송된 본문에서 `@` 없이 이름 색상으로 표시. 기존 저장 본문·멘션 ID·AI 요청 계약은 유지한다.
- [fix] `@` 후보 목록에서 ↑↓ 순환·Enter 선택·Esc 닫기를 지원하고 활성 후보를 강조·스크롤한다. `@`만 입력했을 때의 Enter도 선택으로 바꿔 의도치 않은 전송을 방지한다. 한글 조합 중 Enter는 선택·전송하지 않는다.

## 0.2.2 — 2026-09-11

- [feat] 메시지 반응 팔레트를 Microsoft 공식 Fluent Emoji 3D PNG 40종으로 교체. 기존 유니코드 반응 값과 과거 반응 표시 호환성을 유지하고, 빠른 반응·전체 선택기·반응 칩에 같은 자산을 사용한다.
- [release] 운영 웹 `20260910-02`와 Mac arm64/x64·Windows x64 `0.2.2`, 두 업데이트 피드를 함께 배포. 서버·MCP·공용 계약은 직전 릴리스와 동일하며 DB 변경 없음. VM 격리 DB 서버 94개, 웹 159개·데스크톱 15개 테스트와 전체 타입 검사·빌드 통과. 공개 웹 자산·설치 파일 해시, Mac Developer ID 서명·Apple 공증·Gatekeeper, health·인증·404 계약 확인.

## 0.2.1 — 2026-09-10

- [feat] macOS Dock·Windows 작업 표시줄에 전체 대화의 안 읽은 글 수 배지를 추가. 기존 서버 카운트·React Query 실시간 갱신을 재사용하고 읽음·삭제·탈퇴·재접속·로그아웃을 반영한다. Windows는 100개 이상을 99+로 표시한다.
- [fix] 데스크톱 창이 뒤에 떠 있을 때 현재 방의 새 글이 자동으로 읽음 처리되어 배지가 사라지지 않도록 포커스 복귀 때 읽음 처리한다.
- [test] 웹 159·데스크톱 15·MCP 2·실행기 6개와 VM 격리 DB 서버 94개(총 276개), 전체 타입 검사·빌드 통과. 실제 Electron preload→Dock 숫자 표시/해제와 Windows 오버레이 이미지 생성 검증 통과.
- [release] 운영 웹 20260910-01·Mac arm64/x64·Windows x64 0.2.1과 업데이트 피드 배포. Mac 서명·공증·Gatekeeper, 패키지 소스 일치·피드 5개 파일 해시·공개 다운로드 검증. 이전 릴리스·DB 백업·구버전 다운로드 보존. [검증·적용 조건](handoff/2026-09-10-dock-badge.md).

## 0.2.0 — 2026-09-08

- [docs] 방별 Codex 대화 유지·개인 이어가기 계획 작성. 공식 CLI/SDK/App Server 비교와 실제 호환성 실험·접근 해제·기록 분리 검증을 선행 조건으로 명시. 기능 구현 전.

- [release] PR #14·#15·#16 리뷰/머지 후 운영 서버·웹·Mac/Windows 앱과 업데이트 피드를 배포. 백업 복원 DB와 운영 DB의 migration 4→8 데이터 보존 확인.
- [test] 실제 Mac 0.1.2→0.2.0 업데이트·로그인 유지, 설치 앱의 Codex/Claude 텍스트 첨부 답변·암호화 저장·재실행 복구 통과. Windows/Intel 실기 검증은 남아 있다. [배포 결과](handoff/2026-09-08-ai-invite-release.md).

- [fix] AI 요청 폼의 재시도 버튼이 기존 요청 재전송과 새 폼 제출을 함께 실행하던 문제를 수정. 응답 유실 후 같은 질문의 요청 ID를 보존하며 세 회귀 테스트를 추가.

- [fix] 데스크톱 복구 작업의 세대를 검사해 이전 계정의 늦은 인증 응답이 새 계정의 AI를 중단하거나, 로그아웃 후 다음 저장된 AI를 재연결하지 않도록 수정. 두 회귀 테스트 포함.

- [fix] PC 실행 취소 후 남는 하위 프로세스를 재현하고 Execa의 프로세스 트리 종료로 대체. Windows .cmd/공백 경로/빈 인자 검증을 CI에 추가한다.

- [fix] PR #14 리뷰에서 재현한 삭제 질문 원문 노출을 수정. 실행 상태 API는 공용 DTO 필드만 반환하고 prompt를 공개하지 않는다.

- [chore] 감독의 후속 승인에 따라 서버·PC 실행기·채팅 UI를 세 PR로 나눠 리뷰/배포한다. 새 기능이 포함된 데스크톱은 0.2.0으로 준비한다.

## 2026-09-08 — 대화 중 개인 AI 초대·실제 응답 (개발 검증 이력)

- [feat] 초대한 방만 참여하는 SELECTED·기본 AI·한 번 초대·소유자별 내보내기와 시스템 공지 추가. 기존 개인/방 전용 키 권한 유지(ADR 012).
- [feat] PC Codex/Claude 실행기·데스크톱 최초 연결과 암호화 저장·동일 사용자 복구, 웹 실행기 안내 추가.
- [feat] 요청 버튼과 AI 멘션을 실제 모델 실행/같은 방 인용 답변에 연결. 최근 대화·텍스트 첨부 제공, 한 AI당 한 실행·중복 방지·회수/연결 해제 취소·실패 기록/로그 추가.
- [feat] 현재 방의 AI·소유자·응답 상태 표시, 최초 연결 후 복귀, 모바일 목록/대화 전환과 초대 중 초안 보존.
- [test] 전체 270개 테스트·타입·빌드, 두 계정 브라우저에서 Codex/Claude 실제 응답·자료 읽기·멘션·390/360px 통과. 기존 MCP/이미지 붙여넣기 회귀와 5→8 migration 데이터 보존 확인.
- [docs] 사용법·ADR·실행/배포 이관 정리. 운영 배포·새 서명 앱/Windows 실기 검증은 후속 단계.

## 2026-09-08 — 기능별 PR 분리 검증

- 웹 실행·진단, 채널 MCP, 데스크톱·기여 환경, 개인 AI 참여 API, AI 설정·이미지 붙여넣기를 기능별 PR로 나눴다.
- 리뷰에서 발견한 로컬 OAuth 호스트·WebSocket 종료·AI 리터럴 검색·데스크톱 이미지 CSP 문제를 재현·수정했다.
- 각 PR에서 독립 리뷰 2개·교차 판정과 Node 24/26 CI를 확인한 후 순서대로 main에 머지한다. 최종 테스트 245개·타입 검사·빌드 및 실제 브라우저·MCP 검증을 통과했다. 운영 배포는 별도 단계다.

### 2026-09-08

- [docs] “대화 중 버튼 하나로 내 AI 초대”를 우선순위로 반영. Teams·Slack·Codex·Claude 공식 자료와 현재 코드 비교, 방별 초대/실행기/최초 연결의 개발 순서·완료 기준 작성.
- [docs] PC·모바일 AI 초대 HTML 시안 추가. 초대·처음 연결·요청·내보내기·오프라인/실패와 초안 보존을 Chromium에서 확인. 예시 데이터만 사용하며 운영 코드·배포 변경 없음.

- [feat] #8 개인 에이전트를 한 번 등록해 채널만/모든 대화 참여를 선택하고, 각 방에서 제외·재참여하도록 확장. 기존 채널 키는 원래 범위 유지(ADR 011).
- [feat] 1:1·일반 그룹의 AI 연결과 소유자별 관리, MCP 대화 목록·명시적 목적지 선택·참여자 조회 추가.
- [feat] 이미지 클립보드 붙여넣기를 기존 첨부 미리보기·설명·업로드·실패 재전송 흐름에 연결. 일반 텍스트 paste와 새 초안 보존.
- [test] 웹 149·서버 88·MCP 2·데스크톱 6개와 타입 검사·빌드 통과. 격리 DB·두 계정 Chromium에서 실제 단축키 paste·업로드·MCP 이미지 읽기·재전송 및 AI 참여 상태 실시간 반영 확인. 운영 적용 전.
- [test] 기존 4개 migration DB에 채널 키·메시지를 넣고 신규 migration을 적용해 키 해시·방·멤버십·메시지 보존 확인.

- [chore] 공개 개발 기반: Apache-2.0·기여/보안 안내·이슈/PR 템플릿·설치/자체 운영/AI 연결 문서와 GitHub 호스팅 CI 추가.
- [feat] 자체 서버 HTTPS 원점으로 데스크톱을 빌드하고 같은 서버의 업데이트 피드로 패키징. Google 프로젝트·개인 Mac 서명 고정값 제거, 외부 라이선스 고지 포함.
- [fix] 로컬 개발 동시 실행 명령·MCP 프록시 추가. Linux의 localhost 주소 차이를 없애고 OAuth 콜백 포트를 고정하도록 Vite loopback/strictPort 설정.
- [chore] 운영 예제의 도메인·GCP 리소스를 설정으로 분리하고 키·운영 증빙을 공개 후보에서 제외. 기존 VM·배포 앱은 유지.
- [test] Mac Node 26·격리 Linux Node 24에서 전체 232개 테스트·타입·소스 빌드 통과. 실제 MCP 개발 프록시와 자체 서버용 시험 패키지/라이선스·잘못된 업데이트 피드 거부 확인. actionlint·Gitleaks 검사 통과.

- [chore] 버전 간 업데이트를 검증하도록 데스크톱 0.1.2 배포. 실행 코드는 0.1.1과 동일하며 다운로드 페이지에 자동 확인 주기와 설치 절차를 명시.
- [test] 설치된 Mac arm64 0.1.1에서 운영 피드 확인→130,007,142B 다운로드→SHA512 일치→네이티브 설치→0.1.2 정상 재실행·로그인/채팅 유지·최신 버전 표시 확인. Windows 설치 파일 전체 다운로드 검증, 실기 업데이트 결과는 대기.
- [test] 서명된 앱을 변경하지 않고 실제 updater 이벤트를 기록하는 재사용 관측 스크립트 추가. 업데이트 후 디버거 포트 종료 확인.

- [chore] 0.1.1 Mac arm64/x64 공증·Windows x64 제작과 다운로드 공개. 감독 Mac에 arm64 설치·실제 앱 로그인/채널 확인·정상 재실행, 배포 후 백업 완료.
- [test] 공개 arm64 DMG 전체 SHA256, 모든 설치 파일 Range·크기·샘플 바이트, 업데이트 피드/블록맵 SHA256·참조 파일 SHA512 검증. Windows 실기·버전 간 업데이트는 미검증.

- [chore] Desktop OAuth 설정과 서버 20260908-02 적용, 기존 사용자로 실제 Google 로그인·세션 발급 및 두 AI MCP 읽기 검증.

- [fix] 자체 데스크톱 승인 폼을 Google 공식 Desktop OAuth·PKCE와 서버 ID 토큰 검증으로 대체(0.1.1 준비).
- [feat] Google sub 고유 키와 기존 웹 계정 연결 추가. Desktop 설정 없으면 세션 발급·패키징 차단.
- [test] 로컬/VM 격리 DB 서버 각 81개·데스크톱 3개·타입 검사·Chromium 콜백/세션 통과. 실계정 로그인·배포는 대기.

- [investigation] 앱 로그인 승인 폼의 no-referrer 정책이 Origin:null을 만들어 정상 요청을 거부하는 현상 재현.
  로컬 same-origin 수정 후보는 최종 브라우저 복귀 검증에서 시간 초과. 수정·배포 완료 아님.
  Google 공식 Desktop OAuth·PKCE·loopback 및 서버 ID 토큰 검증 방법 조사(ADR 009).

- [feat] Electron 데스크톱 앱·기본 브라우저 로그인·PKCE 일회용 세션 교환·트레이·업데이트 추가.
- [feat] 앱 설치 페이지와 업데이트 파일 제공, 웹 앱 설치 링크·알림 클릭 시 데스크톱 복귀 추가.
- [test] 서버 80개·웹 143개·데스크톱 2개·타입 검사·웹 빌드 통과.

### 2026-09-07

- [feat] 다른 사용자도 로컬 커넥터 설치 없이 연결하도록 `/mcp` 원격 HTTP와 웹의
  Codex 설정·Claude Code 등록 명령 복사를 추가. 채널별 키·회수·격리 유지(ADR 008).
- [test] 원격 MCP 두 사용자 HTTP 검증을 포함해 서버 76개·웹 143개·stdio MCP 2개,
  타입 검사·빌드 통과. VM 격리 PostgreSQL 17 서버 76개 통과.
- [chore] 릴리스 `20260907-04` 배포, 두 AI 키로 공개 HTTPS MCP 도구 목록·채널 읽기·검색
  확인. 배포 186파일·복구용 GCS 압축 파일 SHA-256 검증, 기존 서비스 정상 유지.

- [feat] 초대형 채널과 외부 AI MCP MVP. 채널별 AI 신원·해시 저장 키·90일 만료·회수,
  대화/첨부 읽기·검색·게시와 AI 배지, Codex/Claude Code 등록 도구 추가(ADR 007).
- [test] 웹 143개·서버 73개·MCP 2개·타입 검사·빌드 통과. VM 격리 DB 서버 73개 통과.
- [chore] 릴리스 `20260907-03` 배포, 운영 요구사항 채널(사람 2명·AI 2개)과 초안 생성.
  Codex/Claude Code에 별도 연결 등록, 실제 MCP 초기화·도구 5개·읽기·검색·게시 검증.

- [feat] 앱 전체 로그 하네스: 브라우저 진단 패널·500건 버퍼·수동 업로드/파일 저장,
  API/화면/소켓/DOM/오류/longtask 관측, 서버 요청 trace·Prisma 시간·자원 지표·안전한 오류 로깅.
- [test] 웹 141개·서버 70개, 타입 검사·빌드 통과. 실제 브라우저 600ms 지연 주입에서
  HTTP 602ms·전송→DOM 608ms 관측, 민감 원문 제외 확인. 운영 지연 원인은 아직 미확정.
- [docs] ADR 006·진단 수집/분석 절차와 재사용 브라우저 하네스 추가.

- [chore] 감독 지정 계정 2개를 추가 등록(허용 계정 총 4개), 실행 프로세스 적용과 서버 정상 응답 확인.

- [chore] 감독 지정 두 번째 계정을 운영 허용목록에 추가하고 서비스 재시작 적용.
  기본 신원 범위만 요청하므로 Google 테스트 사용자 등록이 필수라는 이전 안내를 공식 문서에 따라 정정.

- [feat] 각자 인터넷에서 검증하도록 웹을 Electron보다 먼저 기존 gatelab-vm에 배포 —
  `https://deuce.goldenlabs.dev`, 별도 systemd 앱·Cloudflare Tunnel·PostgreSQL DB/역할.
- [feat] Fastify에 빌드된 SPA 제공·페이지 새로고침 지원, API/없는 자산 404 유지.
  서버 기본 바인딩을 loopback으로 하고 종료 시 연결 정리. 배포 라우팅 테스트 추가.
- [chore] Node 24 LTS·고정 pnpm, 앱 CPU/메모리 제한, 일일 GCS 백업·30일 보존 설정.
  SHA-256·별도 DB 복원·첨부/설정 압축 해제 검증 통과.
- [test] 로컬 웹 137개·서버 68개·타입 검사·빌드 통과. 실제 VM의 PostgreSQL 17에서도
  격리 DB로 서버 68개 통과. 공개 HTTP·인증·WebSocket 차단 검증, 기존 리뷰게이트 active 확인.
- [docs] ADR 005·배포 이관 문서 기록. 배포 콜백 URI를 감독이 등록·저장한 뒤
  09:35 재접속에서 `redirect_uri_mismatch` 해소·Google 로그인 화면 진입 확인. 이후 감독의 로그인
  완료 보고와 운영 DB 사용자 1명 생성 확인. 두 사용자 상호 통신은 미검증.

### 2026-09-05

- [fix] PR #7 리뷰의 실패 원문 가로 넘침 수정 — 목록용 nowrap span을 전용 블록으로
  바꿔 본문·파일명을 줄바꿈하고 높이 120px 안에서 스크롤. 실제 Composer와 CSS를
  사용한 Chromium 격리 검증에서 1,600자 실패 시 문서 폭 9,646px 회귀를 재현한 뒤,
  800/1,280px 화면의 긴 본문·4,000자 여러 줄·첨부 캡션 6경로에서 넘침 해소와
  재전송 payload·초안 보존을 확인. 웹 137개·서버 67개 테스트, 빌드·타입 검사 통과.
- [fix] #6 공유 탭에서 같은 방 검색 결과를 누르면 채팅 탭을 열도록 수정 —
  실 Chrome 재현·수정 후 확인 및 회귀 테스트 통과(Codex). 새 테스트의
  Testing Library `exact` 옵션 3곳을 제거해 빌드 실패를 해소(Claude).
- [fix] 타임라인 "이전 메시지 보기" 위치 보정이 무효였던 문제 — `await fetchNextPage()`
  뒤 rAF에서 보정했는데 react-query v5 알림이 setTimeout 배치라 커밋 전에 실행돼
  높이 차가 0이었다(실 Chrome 휠 스크롤로 관측). 페이지 수를 키로 하는
  `useLayoutEffect`에서 커밋 직후 보정하도록 변경. 회귀 테스트(rAF 선행 순서 재현) 추가
- [fix] 다른 탭에서 원본을 수정·삭제하면 그 메시지를 인용한 답장의 인용 본문·삭제
  표시가 갱신되지 않던 문제 — `replaceMessage`가 `replyTo`도 함께 갱신. 테스트 추가
- [fix] 컴포저가 전송·업로드 응답을 기다리는 동안 이어 쓴 글이 완료 시 `setText('')`로
  지워지던 문제 — 상태 소유권을 다시 설계했다(Codex 검수 2회 반영, Fix-Loop 재검토).
  제출 시 payload(본문·파일·답장 대상·멘션 id)를 스냅샷으로 떼어내고 초안(텍스트·파일
  칩·답장 칩)을 즉시 비운다. 텍스트 전송과 첨부 업로드를 하나의 `useMutation`으로 묶어
  요청 중/실패 payload는 그 mutation(`variables`·`isError`·`reset`)만 소유하고, 성공·실패
  콜백은 초안을 건드리지 않는다. 실패하면 안내 바에 원문(파일명 포함)을 보여 「재전송
  (동일 payload)/버리기」만 제공하고, 미해결 실패가 있는 동안은 새 전송을 받지 않는다
  (초안 작성은 계속 가능). 실패 시 입력창 복원 계약은 payload 보관+명시적 재전송으로
  바뀌었다. 업로드 중 파일 선택·드롭 차단은 이유(성공 시 초기화)가 사라져 제거.
  회귀 테스트 7개(지연 실패+새 초안, 미해결 실패 중 전송 차단→해소 후 전송, 버리기,
  재전송의 답장·멘션 보존, 업로드 중 초안 유지, 업로드 실패 재전송, 첨부 재전송 성공 시
  새 초안 파일 유지). 실패 경로는 단위 테스트로만 검증했다
- [fix] 열린 반응 팔레트·더보기 메뉴가 아래 메시지의 액션 바에 가려지던 z-index 문제
- [docs] Claude Chrome 플러그인 실 브라우저 검증 결과 기록 — 같은 계정 두 탭 기준
  메시지 액션 6종·첨부/공유 탭/다운로드·검색 점프·스크롤 3종 통과, 두 계정 필요 항목
  분리. `docs/handoff/2026-09-05-browser-verification-claude.md`
- [docs] ADR 004 — 컴포저 전송 payload 소유권(단일 outgoing mutation·미해결 실패 중 전송
  차단·완료 콜백의 초안 무접촉) 결정과 트레이드오프 기록
- [review] Codex 독립 검수 3회 — 1·2차에서 컴포저 재전송 결함 5건 지적, 3차 최종 PASS
  (독립 재현 5개·웹 137개·빌드·`git diff --check` 통과). 두 실계정 항목 미검증과
  브라우저 실패 주입 미실행은 그대로 남는다. 커밋·push·PR·Electron 착수는 미실행
- [docs] 수동 검증 재개 기록 — 기존 개발 DB의 compose 이름과 포트 중복 주의,
  서버·웹 기동 및 HTTP 확인 결과를 남겨 다음 검증의 시작점을 명확히 했다.

### 2026-08-30

- [fix] 웹 SPA 후속 정리(계획 ④ 최종 리뷰 파킹분) — 실패 피드백 일원화:
  반응·고정·삭제·음소거·그룹 관리 실패 시 `ErrorNotice` 안내(404 액션 실패는
  문구 없이 재조회 유지), 대화 상세·타임라인·활동 조회 실패 시 안내 +
  "다시 시도" 버튼, 검색 로딩·실패 구분
- [fix] 컴포저 정밀 수정 — 멘션 삽입 후 캐럿을 멘션 바로 뒤로 복원, `@` 단독
  입력의 Enter는 전송으로, `collectMentionIds` 접두 중복 이름 정확 매칭
  (긴 이름 우선, renderMentions와 동일 규칙), 본문 앞뒤 공백 trim 전송
- [fix] URL 끝 문장부호(`.,;:!?`)를 링크에서 제외, 고정/해제 성공 시 대화
  상세를 즉시 재조회(고정 배너 지연 제거), `attachRealtime`이 detach를
  반환해 소켓 리스너를 개별 해제(`socket.off()` 전체 해제 제거)
- [fix] 다이얼로그 접근성 — 새 채팅·그룹 설정에 `role="dialog"`·
  `aria-modal`·레이블·Escape 닫기
- [test] 픽스처 `msg`를 `test/fixtures.ts`로 분리 — cache 테스트 6개가
  import 파일마다 재실행되던 문제 해소(수집 수 == 고유 수). `maybeNotify`
  삭제 메시지·클릭 열기, `attachPresenceSignals` 신호·해제 테스트 보강
- [fix] 최종 리뷰 수정 웨이브 — 대화 상세 재조회를 고정(`/pin`) 경로로
  한정(반응·수정·삭제까지 매번 상세를 재조회하던 낭비 제거, 반응 경로
  회귀 테스트 추가), 첨부 캡션도 본문과 동일하게 trim 전송

### 2026-08-29

- [docs] v1 스펙 확정 — 골든랩 내부용, Teams '채팅' 탭 재현(1:1·그룹 채팅,
  인용 답장, 고정, 읽음 커서, 검색, 프레즌스) + 프로젝트 사이트 iframe 임베드
  모듈. 실사용 스크린샷 확인 결과 팀/채널은 거의 안 쓰여 v2로 이연.
  `docs/design/2026-08-29-deuce-v1-spec.md`
- [docs] ADR 001 — TS 풀스택 모노레포(Fastify·Socket.IO·Prisma·PostgreSQL,
  React·Vite·react-query) + Electron 데스크톱. Go 서버·Mattermost 배포 기각
- [docs] ADR 002 — 임베드는 iframe 방식만. 리뷰게이트 뷰어의 iframe +
  postMessage 검증 패턴 재사용, same-site 쿠키로 호스트 인증 코드 0줄

- [feat] `Deuce` 프로젝트 초기화 — Microsoft Teams와 같은 기능 범위의 팀 커뮤니케이션·협업 프로그램을 위한 control repo와 문서 SSOT 생성
- [chore] GOLEM 카탈로그 등록 및 골든노트 연결 활성화 — 프로젝트 위치와 상태 문서의 중앙 탐색 경로 마련

- [feat] 서버 파운데이션 모노레포 부트스트랩 — pnpm workspace(`apps/server`,
  `packages/shared`), Fastify 5 ESM(NodeNext) 서버 스켈레톤, 공통
  tsconfig 베이스
- [feat] Prisma 스키마 — v1 채팅 도메인(User·Conversation·Message·
  Attachment·Reaction·ReadState·Mention) 모델링과 초기 마이그레이션,
  로컬 개발/테스트용 Postgres 16 docker-compose
- [feat] 세션·Google OAuth 인증 — `@fastify/secure-session` 쿠키 세션,
  Google 로그인 콜백(허용 이메일 목록 기반 접근 제어), `/auth/me`·
  `/auth/logout`
- [chore] Prisma·`@prisma/client` 6.19.3 정확 고정 — npm latest는
  prerelease(8.0.0-rc)이고 Prisma 7은 기존 `datasource url = env(...)`
  Migrate 설정 방식을 깨뜨려(어댑터·`prisma.config.ts` 필요) 6.x에 머문다.
  사유는 `docs/adr/003-prisma-6-pin.md` 참고
- [fix] 최종 리뷰 수정 웨이브 — `email_verified` 미검증(Critical), 콜백
  예외 메시지 노출, oauthState 재생 공격, 보안 계약(§8) 테스트 공백,
  서버 부팅 경로의 암묵적 env 로딩 등 Critical 1건·Important 5건·
  Minor 4건 수정. 상세는 PR #2 (커밋 9c9e7d0·e40c128) 참고

- [feat] 계획 ② 채팅 REST API — `/api` 인증 가드(fastify-plugin, 허용목록
  매 요청 재검사, 14일 세션 쿠키), 전역 에러 핸들러(5xx 마스킹), 사용자 목록,
  대화방(DM 중복 방지·그룹 생성·이름·멤버·나가기·음소거·읽음 커서), 메시지
  (인용 답장·멘션·(createdAt,id) 키셋 페이지네이션·수정·소프트 삭제·반응·고정),
  대화방 상세(최신 핀), pg_trgm 한국어 부분일치 검색(ILIKE 이스케이프,
  멤버십 스코프), 활동 피드. 실 DB 테스트 46개
- [fix] 계획 ② 최종 리뷰 수정 웨이브 — 세션 쿠키 maxAge 14일(계획 결함),
  DM 나가기 차단(대화 분기 방지), 활동 피드 멤버십 스코프, 신규 멤버 안 읽음
  기준선 joinedAt, 대화방 목록 정렬 회귀 테스트
- [docs] 스펙 §8 보강 — 골든랩 메일은 네이버웍스라 구성원은 개인 구글
  계정으로 로그인, OAuth 동의 화면은 External + 테스트 사용자 방식 확정

- [feat] 계획 ③ 실시간·프레즌스·파일 — Socket.IO(세션 쿠키 인증, user/convo
  룸, 가드된 접속 핸들러), 이벤트 브로드캐스트(message.new/updated/deleted,
  reaction.changed, read.advanced, conversation.created/updated/removed +
  룸 동기화), 인메모리 프레즌스(online/away/offline + GET /api/presence),
  서버 경유 파일 첨부(FileStorage 드라이버·경로 탈출 가드·고아 파일 정리·
  multipart 업로드→메시지·스트림 다운로드·공유 탭). 실 DB·실 소켓 테스트 64개
- [feat] 계획 ② 이월분 정리 — 그룹 타인 제거 엔드포인트, 반응·핀·읽음
  라우트의 DTO 반환 전환, 삭제 메시지 가드, 읽음 커서·안읽음 카운트의
  (createdAt,id) 페이지네이션 정합
- [fix] 계획 ③ 최종 리뷰 수정 웨이브 — 256KB+ 업로드 캡션 소실(멀티파트
  필드 파싱 순서), Socket.IO 이벤트 타입 계약(`ServerToClientEvents` 등
  shared 고정), 확장자 세정(`photo.jpg (1)` 500 방지), 삭제 메시지 첨부
  메타 마스킹, 핸드셰이크 실패 로깅, 다운로드 content-length·nosniff
- [docs] 스펙 §3·§6 문구 — 파일 첨부를 GCS 서명 URL 직행에서 서버 경유 +
  스토리지 드라이버(개발 로컬 디스크, 운영 GCS는 계획 ⑥)로 변경 — 다운로드
  마다 멤버십 검사, 개발·운영 단일 흐름

- [feat] 계획 ④ 웹 SPA — React 19 + Vite + react-query SPA(`apps/web`).
  인증 게이트(`/auth/me` 401 → 로그인 화면)·앱 셸 라우팅, 대화 목록(안 읽음
  필터·배지·미리보기)·새 채팅(DM 중복 방지), 메시지 타임라인(커서
  페이지네이션·읽음 커서 전진)·컴포저(인용 답장·`@` 멘션 자동완성·IME 조합
  가드), 메시지 액션(수정·소프트 삭제·반응 6종·고정 배너), 파일 첨부 업로드·
  다운로드 카드·공유 탭, 그룹 관리(이름·멤버·나가기)·음소거, 타입 계약
  `@deuce/shared` 기반 Socket.IO 와이어링(캐시 직접 갱신 + 재접속 시 전체
  invalidate)·프레즌스 점, 검색·활동 피드·메시지 점프·웹 알림. 모든 응답은
  zod `.parse()` 통과, 상태 동기화는 react-query 단일 경로. 테스트 80개(고유,
  수집 기준 122 — 픽스처를 `cache.test.ts`에서 가져오는 파일마다 그 6개가 함께
  재실행된다)
- [fix] 점프 완료 후 `?m=` 정리 — 스크롤을 실행했거나 더 못 찾아 멈출 때
  `useJumpToMessage`가 완료를 알리고 `ChatView`가 파라미터를 지운다. 안 지우면
  같은 고정 메시지를 다시 눌러도 URL이 그대로라 반응이 없고, 채팅↔공유 탭을
  오갈 때 남은 파라미터가 재점프·재하이라이트를 일으켰다
- [fix] 방 이동마다 소켓이 재연결되던 회귀 방지 — 알림 클릭 이동을 붙이며
  `useNavigate()`를 소켓 effect 의존성에 넣었더니, react-router 7의 `navigate`
  가 `locationPathname`에 묶여 매 경로 변경마다 새 함수가 되어 소켓이
  끊겼다 붙었다(재구독 + 전체 invalidate). ref로 최신 `navigate`만 읽도록
  바꾸고 회귀 테스트(`test/socket.test.tsx`)로 고정
- [feat] 계획 ④ 이월 4건 해소 — 포커스 복귀 시 `presence:active` 재송신(자리
  비움 자가 치유), 멤버 재추가 시 `conversation.created` 중복 수신(invalidate
  멱등으로 무해함을 테스트로 고정), 대화방 403 / 메시지 액션 404 비대칭 처리,
  DM "시작" 버튼 중복 클릭 방지
- [feat] 첨부 캡션 400 계약 — `POST /api/conversations/:id/attachments`의
  캡션이 4000자를 넘으면 무고지 절단 대신 400 `invalid body` + 저장 파일
  정리. PR #4 듀얼 리뷰 이월 사항
- [decision] 멘션은 이름 매칭 방식 — 본문은 평문이라 클라이언트가 전송 직전에
  `@{멤버 이름}` 포함 여부로 사용자 ID를 뽑아 `mentions`에 실어 보내고(서버는
  ID가 멤버인지만 검증), 표시도 같은 멤버 이름 목록으로 강조한다. 본문에 ID
  토큰을 심는 방식은 평문 textarea 입력기와 맞지 않아 v1에서 보류. 이름이
  겹치거나 이름 문자열을 손으로 지우면 어긋날 수 있음을 감수한다
- [decision] 타인 읽음 표시(내 메시지를 누가 읽었는지)는 v1 제외 — 멤버별 읽음
  커서를 돌려주는 REST 스냅샷이 없어 소켓 `read.advanced`만으로는 초기 상태를
  복원할 수 없다. v2에서 `GET /api/conversations/:id`에 멤버별 커서 포함 검토
- [decision] 메시지 점프는 최근 20페이지(약 1,000개)까지만 자동 탐색 — 검색·
  활동에서 온 오래된 메시지는 무한히 거슬러 올라가지 않고 조용히 중단한다.
  전체 탐색은 서버의 "메시지 주변 컨텍스트" 엔드포인트가 필요해 v2로 이연
- [decision] 첨부 캡션 전송은 답장(`replyToId`)·멘션(`mentions`)을 지원하지
  않는다 — 서버 첨부 계약의 공백이다. 확장 여부는 계획 ⑤/⑥에서 결정
