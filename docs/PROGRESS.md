---

## 2026-09-08 — 개인 AI 참여 API

- 개인 에이전트의 CHANNELS / ALL 참여 범위와 대화별 제외·재참여 API를 추가했다.
- 소유자의 현재 사람 멤버십·허용 계정·만료·해제를 모든 MCP 접근에서 확인한다. DM의 사람 멤버 구성은 유지한다.
- 기존 채널 키를 보존하는 마이그레이션과 대화 목록·지정 대화 MCP 도구를 추가했다. 설정 화면과 이미지 붙여넣기는 다음 PR에서 제공한다.

## 2026-09-08 — 데스크톱 앱·기여 환경

- Electron 앱의 Google 네이티브 로그인, 다운로드·업데이트 및 배포 도구를 추가했다.
- Google subject 연결 마이그레이션과 로그인·다운로드 검증을 포함한다.
- 개발·자체 호스팅·기여 문서와 Apache-2.0 라이선스를 추가했다. 저장소 공개 전환·운영 배포는 수행하지 않았다.

## 2026-09-08 — 채널과 외부 AI MCP

- 채널 생성, AI 표시, 채널별 키 발급·해제와 stdio/원격 MCP를 추가했다.
- AI 키는 해시로 보관하며 멤버십·만료·해제를 모든 읽기·쓰기에서 확인한다.
- 채널 API 및 실제 MCP 프로토콜 테스트를 포함한다.

## 2026-09-08 — 웹 실행·진단 기반

- Fastify에서 SPA를 제공하고 loopback 바인딩·종료 처리·배포 설정 예시를 추가했다.
- 요청·소켓·업로드 진단을 연결하고 본문·자격증명을 진단 데이터에서 제외한다.
- Node 24/26 CI와 비밀정보 검사를 추가했다. 각 기능 PR의 테스트·타입 검사·빌드를 검증한다.
lifecycle: active
---

# Deuce 진행 상태

## Bootstrap

- [x] 독립 Git 저장소와 `main` 브랜치 생성
- [x] Claude 문서 SSOT와 Codex 중개 구조 생성
- [x] 진행 상태와 변경 이력 문서 생성
- [x] GOLEM 카탈로그 등록 및 골든노트 연결 활성화

## 제품 정의

- [x] 프로젝트 목적 확정 — Microsoft Teams와 같은 기능 범위의 팀
  커뮤니케이션·협업 프로그램
- [x] 사용자·조직·권한 모델 확정 — 골든랩 내부 단일 조직, Google OAuth +
  허용 이메일 목록 (팀/채널 계층은 v2로 이연)
- [x] 첫 릴리스 범위와 기능 우선순위 확정 — v1은 Teams '채팅' 탭 재현 +
  프로젝트 사이트 임베드 모듈. `docs/design/2026-08-29-deuce-v1-spec.md`
- [x] 기술 스택 결정 — TS 풀스택 모노레포 + Electron (ADR 001),
  iframe 임베드 (ADR 002)
- [ ] Microsoft Teams의 사용자 기능을 기능 맵으로 정리 (v2 범위 선정 시)

## 현재 우선순위

- [x] v1 스펙 기반 구현 계획 수립 (writing-plans)
- [x] 서버 파운데이션(계획 ①) 구현 완료 — pnpm 모노레포, Fastify 서버
  스켈레톤, Prisma 스키마·마이그레이션, 세션·Google OAuth 인증, 리뷰
  수정 웨이브(보안·문서·부팅 검증) 반영
- [x] 계획 ② 대화·메시지 REST + 검색 구현 완료 — 인증 가드(허용목록 매 요청
  재검사·14일 세션), 대화방(DM 중복 방지·그룹·관리·읽음 커서), 메시지(인용·
  멘션·커서 페이지네이션·수정·삭제·반응·고정), pg_trgm 검색, 활동 피드
- [x] 계획 ③ Socket.IO 실시간·프레즌스·파일 구현 완료 — 쿠키 인증 소켓 +
  user/convo 룸, 이벤트 브로드캐스트 9종(타입 계약 `@deuce/shared` 고정),
  인메모리 프레즌스, 서버 경유 파일 첨부(로컬 드라이버·고아 정리·공유 탭),
  이월분(타인 제거·DTO 반환 전환·커서 정합) 포함
- [x] 계획 ④ 웹 SPA 구현 완료 — 인증 게이트·앱 셸, 대화 목록·새 채팅, 메시지
  타임라인·컴포저(인용 답장·멘션 자동완성), 메시지 액션(수정·삭제·반응·고정),
  파일 첨부·공유 탭, 그룹 관리·음소거, 소켓 실시간 반영·프레즌스, 검색·활동
  피드·메시지 점프·웹 알림. 이월 4건 해소(포커스 복귀 시 presence:active
  송신, 멤버 재추가 시 conversation.created 중복 수신, 403/404 비대칭 처리,
  DM 시작 버튼 중복 클릭 방지), 첨부 캡션 4000자 초과 400 계약 추가.
  web 테스트는 픽스처 분리 후 수집=고유 기준으로 셈한다(후속 정리 완료 시점 103개)
- [x] 계획 ④ 후속 정리 완료 — 실패 피드백 일원화(ErrorNotice + 다시 시도),
  컴포저 정밀 수정(멘션 캐럿·단독 @ Enter·접두 중복·trim), URL 끝 문장부호,
  고정 배너 즉시 갱신, 소켓 리스너 개별 해제, 다이얼로그 접근성, 픽스처 분리
  (web 테스트 수집 수 == 고유 수, 실측 103개)
- [ ] 계획 ⑤ Electron 셸 (착수 전 아래 수동 검증 통과 필요)
- [ ] 계획 ⑥ 임베드 + 배포

## 수동 검증 진행 중 (OAuth 발급 완료: 2026-08-31)

2026-09-05 재개: 기존 `server-foundation-postgres-1`이 5434 포트에서 개발 DB를
제공 중이며 사용자 4명을 확인했다. DB를 다시 띄울 때는 먼저 실행 중인 컨테이너를
확인한다(기본 compose 프로젝트로 중복 실행 시 포트 충돌). 서버 4000·웹 5173
기동, 웹 HTTP 200·미로그인 `/auth/me` 401을 확인했다. Chrome 자동화로 한글
메시지 전송·검색·강조를 확인했고, 공유 탭에서 같은 방 검색이 열리지 않는 #6을
재현·수정했다. 이어서 Claude가 Chrome 플러그인으로 검증을 진행해 빌드 실패(테스트
`exact` 옵션)를 해소하고, 실 브라우저에서 결함 4건(과거 로드 위치 보정 무효·인용 답장
동기화 누락·전송 대기 중 입력 소실·팝오버 z-index)을 발견·수정했다. 컴포저 수정은
Codex 검수 2회에서 재전송 상태 결함이 이어져 Fix-Loop 재검토를 거쳐 단일 outgoing
mutation 소유권 설계로 다시 만들었다. 웹 테스트 19파일 137개·빌드 통과(2026-09-05
12:40 기준). Codex 독립 검수 최종 PASS(독립 재현 5개·`git diff --check` 포함).
변경 6커밋을 push해 PR #7을 생성했고, 첫 branch-cleanup 리뷰의 WARNING(실패 원문의
가로 넘침)을 전용 줄바꿈·스크롤 블록으로 수정했다. 실제 Composer·CSS를 사용한
Chromium 격리 실패 주입 6경로에서 넘침 해소·재전송 payload·초안 보존을 확인했고,
웹 137개·서버 67개 테스트와 빌드·타입 검사를 통과했다. 재리뷰·머지 결과는
[PR #7](https://github.com/thissak/Deuce/pull/7)에서 확인한다. Electron 착수는 미실행.
두 실계정 항목(unread·프레즌스·알림·실계정 활동)과 실제 서버·앱 전체 흐름의
브라우저 실패 주입은 미검증으로 남아 있다.
상세: `docs/handoff/2026-09-05-browser-verification-claude.md`.

계획 ④ 산출물은 실 브라우저 검증이 남아 있다. 계획 ⑤ 착수 전에 감독과 함께
`pnpm --filter @deuce/server dev` + `pnpm --filter @deuce/web dev`로 확인한다.

OAuth 클라이언트는 발급을 마쳤다 — GCP 프로젝트 `deuce-507212`, 클라이언트
`Deuce Local`(웹 애플리케이션), 리디렉션 URI `http://localhost:5173/auth/google/callback`,
게시 상태 '테스트 중' + 테스트 사용자 `thissak@gmail.com`. `apps/server/.env`의
네 항목(클라이언트 ID·시크릿·콜백 URL·`ALLOWED_EMAILS`)도 실제 값으로 채웠다.

현재 허용 계정이 하나뿐이라 대화 상대를 만들려고 개발 DB에 더미 사용자 3명
(`김철수`·`김철`·`이영희`, `@example.com`)을 직접 넣어 두었다. 이들은 실제로
접속하지 않으므로 **실시간 상호 반영·프레즌스·웹 알림은 이 상태로 검증할 수
없다** — 두 번째 구글 계정을 테스트 사용자와 `ALLOWED_EMAILS` 양쪽에 추가한
뒤 확인한다. 검증이 끝나면 더미 사용자는 지운다.

- [x] Google 로그인 — 허용 계정 성공(사용자 생성·세션 진입 확인). 비허용 계정
  403은 미확인
- [x] 대화방 생성·메시지 전송 (DB에 대화방 2·메시지 2 확인)
- [ ] 브라우저 2개(서로 다른 허용 계정)에서 메시지·답장·수정·삭제·반응·고정이
  새로고침 없이 상대 화면에 반영 — **같은 계정 두 탭**에서는 생성·답장·수정·
  삭제·반응 추가/해제·고정/해제 모두 소켓으로 즉시 반영 확인(2026-09-05, Chrome
  플러그인). 인용 답장의 원본 수정·삭제 반영은 결함 수정 후 통과. 서로 다른
  계정은 미검증
- [ ] 다른 방을 보는 중 수신 시 목록 배지 증가 → 방 진입 시 해소, 탭 숨김 시
  자리 비움 점 → 포커스 복귀 시 온라인 — 두 번째 실계정 필요, 미검증
- [x] 파일 첨부(캡션 포함) 전송 → 첨부 카드 → 공유 탭 → 다운로드 — 캡션+txt
  첨부 전송·카드·공유 탭 목록·두 탭 반영, 첨부 링크 클릭으로 `~/Downloads`에 받은
  파일과 서버 저장 파일 모두 원본과 sha256 일치(102B)
- [x] 한국어 키워드 검색 → 결과 클릭 시 해당 방 이동·하이라이트 (실계정, 다른
  방·같은 방·공유 탭 경로 모두 통과)
- [ ] 활동 탭의 멘션·반응 항목 클릭 점프 — 상대 사용자의 반응이 필요해 개발 DB에
  더미 사용자 반응 1건을 시드해 항목 표시·클릭 점프·강조를 확인 후 삭제.
  실계정 상대의 반응·멘션으로는 미검증
- [ ] 탭 숨김 상태 수신 시 웹 알림 표시, 클릭 시 해당 방 이동, 음소거 방은
  알림 없음 — 두 번째 실계정 필요, 미검증
- [x] 타임라인 스크롤 3종 — ①은 결함(보정 무효) 발견·수정 후 통과, ②③ 통과.
  같은 계정 두 탭·실제 휠 스크롤로 확인(Chrome 플러그인 `javascript_tool`의 `scrollTo`는
  offset만 바뀌고 scroll 이벤트가 관측되지 않아 오탐 — 도구 한정 관측이며 일반
  Chromium에서는 발생함을 Codex가 확인).
  jsdom은 레이아웃을 계산하지 않아 ①만 높이를 흉내 낸 단위 테스트로 보강
