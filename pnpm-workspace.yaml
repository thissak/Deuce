packages:
  - "apps/*"
  - "packages/*"

# allowBuilds permits esbuild's postinstall script to run (required for vitest's bundler).
# This is the canonical pnpm 11 mechanism for managing build script permissions.
allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  prisma: true
